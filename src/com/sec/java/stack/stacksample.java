package com.sec.java.stack;

public class stacksample {

	/*public static void main (String args[])
	{
		ArrayStack ars = new  ArrayStack();
		ars.push(4);
		ars.push(1);
		ars.push(2);
		ars.push(5);
		ars.pop();
		if(!ars.isEmpty()){
			Object res = ars.pop();
			int resultant;
			if(res instanceof java.lang.Integer)
				 resultant = (Integer)res;
			//System.out.println("res:"+res);
		}
	}*/
}
